# 域名配置
PRIMARY_DOMAIN=localhost
SECONDARY_DOMAIN=localhost

# SSL 证书邮箱 - 请替换为您的邮箱
SSL_EMAIL=admin@colletools.com

# 应用配置
NODE_ENV=production
PORT=3000

# 文件上传配置
MAX_FILE_SIZE=100MB
UPLOAD_PATH=./uploads

# 日志配置
LOG_LEVEL=info
LOG_PATH=./logs

# 部署配置（避免冲突）
DEPLOY_ID=local-test
HTTP_PORT=8081
HTTPS_PORT=8443
# 如果80/443被占用，可以使用：
# HTTP_PORT=8080
# HTTPS_PORT=8443